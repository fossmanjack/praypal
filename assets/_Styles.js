import { StyleSheet } from 'react-native';

export default const _Styles = StyleSheet.create({
	dark: {
		backgroundBlurHash: {
			...StyleSheet.absoluteFill,
			width: '100%',
			height: '100%'
		},

	},
	light: {
		backgroundBlurHash: {
			...StyleSheet.absoluteFill,
			width: '100%',
			height: '100%',
		},

	},
});
